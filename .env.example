# Configuration Vite / React

# API Base URL
# En développement: Vite proxy automatiquement vers fcchiche.fr via la config vite.config.js
# En production: URL relative (même domaine)
VITE_API_BASE=/api

# Environnement
NODE_ENV=development
